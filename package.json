{
  "name": "dual-assets-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Радар котировок Bybit Dual Assets BTC/USDT: эффективный APR, вероятность конвертации, подбор Sell High",
  "scripts": {
    "test": "node --use-env-proxy scripts/selftest.mjs",
    "test:offline": "node scripts/selftest.mjs --offline",
    "audit": "node --use-env-proxy scripts/audit.mjs",
    "record": "node --use-env-proxy scripts/record.mjs --out data-branch",
    "backtest": "node --use-env-proxy scripts/backtest.mjs",
    "serve": "python -m http.server 8642 --directory web"
  },
  "engines": {
    "node": ">=24"
  }
}
